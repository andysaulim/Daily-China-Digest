# anthropic 1.x is required, not merely allowed. digest.py uses adaptive
# thinking, output_config.effort and the Claude 5 model IDs, none of which
# exist on 0.x. The old >=0.39.0 floor resolved to a version that worked
# only by accident.
anthropic>=1.0.0
feedparser>=6.0.0
requests>=2.31.0

# httpx is NOT pinned here on purpose. anthropic 1.x brings its own backend
# (httpx2) and digest.py resolves whichever is present at import time.
